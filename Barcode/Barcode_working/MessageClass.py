class MessageClass:
    def __init__(self, data = object, message = object, status = object, level = object, name = object):
    self.data    	= data
    self.message 	= message
    self.status		= status
    self.level		= level
    self.name		= name
